(tcp-server :port 55955)

(let [client (tcp-client :port 55956)]

(streams
  (with {:metric_f 1 :host nil :service "events/sec"}
    (rate 5
       (fwd client)))

  (where (service #"^per")
    (percentiles 5 [0 0.5 0.95 0.99 1]
      (fwd client))))
)
