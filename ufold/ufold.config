(tcp-server :port 55955)

(let [client (tcp-client :port 55956)]

(streams
  (with :metric_f 1
    (with :host nil
      (with :service "events/sec"
        (rate 1
          (fwd client)
           prn))))

  (where :service #"^per"
    (percentiles 5 [0 0.5 0.95 0.99 1]
      (fwd client))))

)
